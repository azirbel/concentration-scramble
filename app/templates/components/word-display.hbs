<div class="word-display">
  {{#each letters as |lt|}}
    <div class="letter {{if lt.hidden 'hidden'}} {{if lt.guessed 'guessed'}}">
      <div class="character">
        {{lt.character}}
      </div>
    </div>
  {{/each}}
</div>
